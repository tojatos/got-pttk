version: '3'

services:
  django:
    build: ./got-pttk-back
    command: python manage.py runserver 0.0.0.0:8028
    ports:
      - "8028:8028"
        #frontend:
        #  build: ./frontend
        #  command: ["npm", "start"]
        #  volumes:
        #    - ./frontend:/app/frontend
        #    - node-modules:/app/frontend/node_modules
        #  ports:
        #    - "3000:3000"

        #  volumes:
        #    node-modules:gg=G
